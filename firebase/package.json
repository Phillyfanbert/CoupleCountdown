{
  "name": "couplecountdown-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Local emulator tests for the CoupleCountdown Firestore Security Rules (see ../DESIGN.md §5.3)",
  "type": "commonjs",
  "scripts": {
    "test": "firebase emulators:exec --project demo-couplecountdown --only firestore \"mocha test/**/*.test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-tools": "^15.27.0",
    "mocha": "^11.8.0"
  }
}
